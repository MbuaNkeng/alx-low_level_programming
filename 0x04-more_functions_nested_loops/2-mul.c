#include "main.h"
/**
 * mul - multiply 2 int.
 * @a: The number to be checked.
 * @b: The number to be checked.
 * Return: result for multiplying a and b.
 */
int mul(int a, int b)
{
	return (a * b);
}
