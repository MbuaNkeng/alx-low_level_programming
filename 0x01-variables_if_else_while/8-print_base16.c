#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Aways 0 (Success)
 */
int main(void)
{
	int n;
	int n1;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (n1 = 97; n1 <= 102; n1++)
	{
		putchar(n1);
	}
	putchar('\n');
	return (0);
}
