#include "main.h"
/**
 * int mul(int a, int b) - checks product of 2 numbers.
 * @a: The number to be checked.
 * @b: The number to be checked.
 * Return: result for multiplying a and b.
 */
int mul(int a, int b);
{
	return (a * b);
}
